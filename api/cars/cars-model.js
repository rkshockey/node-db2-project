const db = require('../../data/db-config')

const getAll = () => {
  return 'getAll wired'
}

const getById = () => {
  return 'getById wired'
}

const create = () => {
  return 'create wired'
}

module.exports = {
  getAll,
  getById,
  create
}